import 'package:flutter/material.dart';

/// Colori brillanti ufficiali dei progetti
class ProjectColors {
  // CreditForm - arancio tenue
  static const Color form = Color(0xFFFFA726);

  // CreditJob - verde brillante
  static const Color job = Color(0xFF00C4B3);

  // CreditCalc - celeste brillante
  static const Color calc = Color(0xFF00B0FF);

  // Area riservata - blu brand
  static const Color area = Color(0xFF1565C0);

  // Sfondo neutro
  static const Color background = Colors.white;

  // Grigio molto chiaro per menù
  static final Color menuBackground = Colors.grey[100]!;
}
